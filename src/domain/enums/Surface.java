package domain.enums;

/**
 * Énumération représentant les surfaces de jeu des tournois du Grand Chelem.
 * Chaque ville a sa surface spécifique.
 *
 * @author Salah eddine & james 
 * @version 1.0
 */
public enum Surface {
    /** Surface de l'Open d'Australie (Melbourne). */
    PLEXICUSHION,
    
    /** Surface de Roland Garros (Paris). */
    TERRE_BATTUE,
    
    /** Surface de Wimbledon (Londres). */
    GAZON,
    
    /** Surface de l'US Open (New York). */
    DECOTURF
}